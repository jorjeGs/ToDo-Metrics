export const createTable = (table) => {
    //here would be the code to save the table in the database
    //but for now, as we are locally storing the data in localStorage, we just return the table
    return table;
}

export const deleteTable = (id) => {
    //here would be the code to delete the table in the database
    //but for now, as we are locally storing the data in localStorage, we just return the table
    return id;
}

export const updateTable = (table) => {
    //here would be the code to update the table in the database
    //but for now, as we are locally storing the data in localStorage, we just return the table
    return table;
}